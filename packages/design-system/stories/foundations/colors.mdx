import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";

import { colorsMap } from "./utils.ts";

# Colors

<ColorPalette>
  {colorsMap.map((item) => (
    <ColorItem key={item.title} title={item.title} colors={item.colors} />
  ))}
</ColorPalette>
